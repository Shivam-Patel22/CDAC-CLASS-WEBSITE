#!/bin/sh
# Check if the third argument is 1 (meaning it was a branch checkout, not a file checkout)
if [ "$3" = "1" ]; then
    echo "Applying Django migrations after branch change..."
    python manage.py migrate
    echo "Migrations applied successfully!"
fi
